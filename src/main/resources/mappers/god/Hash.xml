<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="GodHash">
	<insert id="JHinsertHash" parameterType="GodHash">
		insert into hashtag
		(hname, hbcode)
		values(#{hname}, #{hbcode})
	</insert>
</mapper>